# NotesHub
A Web Application to store your notes in a server and retrieve them whenever needed.

## Notes on Forking
When you fork a Notebook (Subject Note), it creates a new empty notebook under your account that retains the metadata and reference back to the original. Note that the notes inside the original notebook are **not copied** into the fork. The newly forked notebook starts empty so you can build out your own structure.
